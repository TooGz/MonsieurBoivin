# Development configuration. Disable this by running: hugo server -e production
# https://gohugo.io/quick-reference/glossary/#environment

# No project-specific development overrides are currently needed.
